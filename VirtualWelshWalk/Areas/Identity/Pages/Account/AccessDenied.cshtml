@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<header>
    <h1 class="text-danger">@Localizer[@ViewData["Title"].ToString()]</h1>
    <p class="text-danger">@Localizer["You do not have access to this resource"].</p>
</header>
